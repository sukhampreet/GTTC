import type { FireReportSummary } from '@/modules/reports/types';

export const fireReportSummary: FireReportSummary = {
  fireAlarms: 3,
  smokeAlerts: 7,
  heatAlerts: 2,
  mcpEvents: 1,
  faults: 4,
  resolvedAlarms: 12,
};
